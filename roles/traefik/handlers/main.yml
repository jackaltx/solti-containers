---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: yes
    scope: user

- name: restart traefik
  ansible.builtin.systemd:
    name: "pod-traefik"
    state: restarted
    scope: user
  listen: "restart traefik"